info:
  name: Create New Price
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/api/shops/:shopId/prices"
  params:
    - name: shopId
      value: 6a3cded8705669a8f5981125
      type: path
  body:
    type: json
    data: |-
      {
        "name": "A4-CL-DS",
        "rate": 20,
        "keys": {
          "colored": true,
          "pageType": "A4",
          "sidedness": true
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
