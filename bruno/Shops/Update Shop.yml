info:
  name: Update Shop
  type: http
  seq: 2

http:
  method: PUT
  url: "{{host}}/api/shops/:id"
  params:
    - name: id
      value: 6a3cded8705669a8f5981125
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Ahad Print Shop",
        "address": "Westridge 3, Rawalpindi",
        "capabilities": ["bw", "a4"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
